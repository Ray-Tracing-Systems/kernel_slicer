{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "NinjaBuild (debug)",
            "type": "shell",
            "command": "cd ../../build && ninja -j 8",
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        },

        {
            "label": "NinjaBuild (release)",
            "type": "shell",
            "command": "cd ../../build-release && ninja -j 8",
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}