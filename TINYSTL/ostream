#pragma once

#include "FILE.h"
#include "stdio.h"

namespace std
{
  struct ostream 
  { 
    void flush();
    template<class T> ostream& operator<<(T x);
  };

  template<class CharT,class Traits> class basic_ostream;
	template<class CharT,class Traits> class basic_istream;
};

